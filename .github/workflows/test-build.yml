name: Test Build

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build and run tests
        run: |
          chmod +x ./gradlew
          ./gradlew build --no-daemon

      - name: Upload test build
        uses: actions/upload-artifact@v4
        with:
          name: factory-hud-goggles-${{ github.sha }}
          path: build/libs/factory_hud-*.jar
          if-no-files-found: error
          retention-days: 14
